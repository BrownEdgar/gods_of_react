import { createSlice } from "@reduxjs/toolkit";

const usersSlice = createSlice({
    name: 'users',
    initialState: 0,
    reducers:{
        saveUsers(statem, action){
            
        }
    }
})
export default usersSlice.reducer;
